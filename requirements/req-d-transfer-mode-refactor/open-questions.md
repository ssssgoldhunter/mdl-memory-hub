# 需求 D 待确认问题

## task 扫描与失败重试

- `02` 模式下待划付记录的状态机如何定义
- 单笔失败后的自动重试边界如何设计
- Web 手动补偿入口的触发条件和权限控制如何设计

## 账户变动复用范围

- `02` 模式 task 执行内部上下账时，哪些账户处理逻辑可以直接复用现有 `01` 划付实现
- 哪些部分需要拆成新分支

## 冻结主流水号生成规则

- `isFrozen=true` 时，`F.trans_no` 的生成规则是否完全复用现有冻结流水生成规则
- 是否需要与划付主流水、转账主流水建立显式映射字段
