# 需求 C 待确认问题

## trans_frozen_t 字段承载方式

- 当前已确认 `F` / `UF` / `D` 的类型取值
- 仍需确认：
  - `org_trans_no` 是否需要新增索引以及索引形式
  - `frozen_flag` 在新模型中保留何种兼容语义

## C 的 API 颗粒度

- 是否沿用现有冻结、解冻两个入口
- 是否保留批量解冻能力
- 批量场景下返回结构是否需要新增“每笔原冻结剩余额度”信息
