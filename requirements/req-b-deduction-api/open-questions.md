# 需求 B 待确认问题

更新时间：2026-03-31 10:18:00 CST

> 说明：本文件只保留仍未最终拍板的点。  
> 已经确定的内容请以 `summary.md` 为准，不要再按这里的旧问题反推实现。

## 接口与字段裁剪

- 扣款接口请求对象相对消费面密对象具体裁剪哪些字段
- 哪些消费字段需要保留为兼容查询、对账、报表

## after 账户处理细节

- 后续是否还需要把 `useFrozen=false/true` 的公共部分继续下沉收敛

## 参数与展示配套

- `bas_param_t` 中：
  - `PARAM_PAY_TRANS_TYPE` 是否已补 `DC`
  - `PARAM_REC_TRANS_TYPE` 是否已补 `DR`

## 清结算通知 TODO

- 扣款成功后需要通知清结算系统
- 当前先按 TODO 设计，待清结算接口与通知结构体明确后实现
- 通知方式已确认：
  - 不阻塞主交易成功返回
  - 本地先落待通知任务
  - 异步持续通知
  - 通知失败或没有最终结果时持续重试
  - 直到拿到明确成功结果为止
