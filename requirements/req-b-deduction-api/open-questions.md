# 需求 B 待确认问题

## 接口与字段裁剪

- 扣款接口请求对象相对消费面密对象具体裁剪哪些字段
- 哪些消费字段需要保留为兼容查询、对账、报表

## after 账户处理细节

- `useFrozen=false` 时是否完全复用现有普通 04 扣款 after 逻辑
- `useFrozen=true` 时付款卡、收款卡账户变动是否需要抽成独立 after 分支

## 异步上账复用范围

- `需求 B` 复用消费异步上账时，哪些类可以直接复用
- 需要新增哪些扣款专用状态或明细字段
