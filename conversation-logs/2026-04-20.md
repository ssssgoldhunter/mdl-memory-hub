# 2026-04-20 对话日志

> 本文档记录 2026-04-20 当天围绕 `mdl-memory-hub` 与 `mdl` 主源码仓的扫描、代码口径核对，以及文档状态同步的关键结论。

## 主题

`mdl-memory-hub` 记忆体仓与 `mdl` 主项目代码状态对账：

- 扫描 `mdl-memory-hub` 的目录结构、需求文档、bug 清单与基线文档
- 对照 `mdl/` 主仓确认 `B/C/D/E/front` 的真实代码实现状态
- 修正文档中已落后于源码的 bug 状态与需求/基线口径

## 本轮关键结论

### 1. `mdl-memory-hub` 是文档/记忆中枢，不是业务源码仓

- `mdl-memory-hub` 用于沉淀：
  - 架构文档
  - 需求口径
  - bug 台账
  - 会话日志
  - 当前实现基线
- 实际业务代码仍在：
  - `/Users/limeng/workspaces/IdeaProjects_mdl_dep/mdl`

### 2. 主仓 `mdl/` 与记忆体的大方向描述基本一致

- 主仓是 Maven 多模块单仓
- 核心模块包括：
  - `fund-catering-base`
  - `fund-catering-consume`
  - `fund-catering-front`
  - `fund-catering-task`
  - `fund-catering-data-batch`
  - `fund-catering-management`
  - `fund-catering-report`
- 关键技术栈与记忆体一致：
  - Java 17
  - Spring Boot 3.2.4
  - LiteFlow 2.12.4.1

### 3. `B/C/D/E` 当前真实代码状态

#### B 扣款

- 入口在 `fund-catering-consume`
- 核心链与普通消费分开
- 主交易类型为 `D`
- 账户变动明细类型为 `DC/DR`
- `useFrozen=false` 与 `useFrozen=true` 已分流实现
- service 入口已先锁付款卡、收款卡
- 成功后仍发 `RocketMQ` 到 `front`

#### C 原冻结额度池

- 已抽出公共冻结池能力 `FrozenPoolHelper`
- 解冻使用 `orgTransNo`
- 冻结扣款使用 `orgFrozenTransNo`
- 支持部分解冻、部分冻结扣款
- `allowFlag/freezableAmt` 的额度池语义已在代码中落地

#### D 划付改造

- `TRANSFER_MODE_SWITCH` 已接入
- `02` 模式为 task 驱动，不再侵入原转账链
- `fund-catering-task` 中 `TransferTi02TaskJobService` 负责扫描与单条执行
- 成功后仍发 `RocketMQ` 到 `front`

#### E 批量扣款

- 接口阶段预落地 + task 单条执行模型已落地
- `DeductionBatchTaskJobService` 负责分页扫描和逐条执行
- `processDeductionDetail02` 会锁付款卡、收款卡，冻结模式额外锁原冻结流水
- 代码链路与单笔 `B` 隔离

### 4. `front` 的真实通知口径已比旧文档更靠前

- `B/D/银行实收` 仍保留 `RocketMQ -> front` 结构
- 但 `front` 消费者当前已不是“待接清结算 API TODO”
- 实际代码中：
  - 扣款消费者已调用 `resultNotifyApi`
  - 划付消费者已调用 `resultNotifyApi`
  - 银行实收消费者也已调用 `resultNotifyApi`
- 普通消费、充值等链路仍兼容 `notifyUrl` 回调
- 提现结果通知仍沿用现有回调链路

### 5. 已确认一组 bug 文档状态滞后

原本仍标记为“确认修复”的 5 个问题，经源码核对后均已实际修复：

- `B-006`
- `BC-006`
- `BC-007`
- `D-011`
- `E-023`

## 本轮实际回写的文档

### 1. bug 状态同步

已更新：

- `mdl-memory-hub/bugs/README.md`
- `mdl-memory-hub/bugs/B-006-deduction-no-mac-idempotent.md`
- `mdl-memory-hub/bugs/BC-006-findfirst-no-empty-check.md`
- `mdl-memory-hub/bugs/BC-007-liteflow-cause-null.md`
- `mdl-memory-hub/bugs/D-011-process-detail-no-result-check.md`
- `mdl-memory-hub/bugs/E-023-notify-consume-null.md`

变更：

- 上述 5 个问题状态统一从 `确认修复` 改为 `已修复`

### 2. requirements 文档同步

已更新：

- `mdl-memory-hub/requirements/overview.md`
- `mdl-memory-hub/requirements/req-b-deduction-api/summary.md`
- `mdl-memory-hub/requirements/req-d-transfer-mode-refactor/summary.md`

变更：

- 修正 `B` 入口描述为代码实际路径 `/consume/trans/transDeduction`
- 修正 `B/D/front` 关于“待清结算 API TODO”的旧口径
- 明确 `front` 扣款/划付消费者当前已直接调用 `resultNotifyApi`
- 补齐更新时间

### 3. superpowers 基线文档同步

已更新：

- `mdl-memory-hub/docs/superpowers/mdl-supply-chain-abcd/00-current-baseline.md`
- `mdl-memory-hub/docs/superpowers/mdl-supply-chain-abcd/01-code-state.md`

变更：

- 修正 `B/D/银行实收` 的 `front` 通知状态，不再写成 `TODO`
- 修正 `E` 代码状态中关于 `front` 扣款消费者的旧描述
- 删除 `00-current-baseline.md` 中已经不成立的
  - `B` 清结算通知接口未完成
  - `D` 清结算通知接口未完成
- 补齐更新时间

### 4. 仓库 README 更新记录同步

已更新：

- `mdl-memory-hub/README.md`

变更：

- 新增 `2026-04-20` 一条更新记录，说明本次“文档与代码状态同步”

## 当前仍需注意的点

### 1. `E` 的实现状态仍带过渡性

- 虽然 `front` 扣款消费者已能调清结算接口
- 但 `E` 相关代码里仍能看到过渡期注释与旧口径残留
- 后续如果继续收口 `E`，仍建议以源码为准再做一次专项核对

### 2. `A` 仍是本轮最明确的未完成项

- `withDrawRuleCheck` 已接入链路
- 但真实清结算查询与拦截能力仍未完成

### 3. 仍未做自动化测试补齐与联调验证

- 本轮工作以“代码阅读 + 文档校对”为主
- 没有新增业务代码
- 也没有补自动化测试或真实联调记录

## 下次续接建议

1. 如果继续做文档治理：
   - 继续扫描 `docs/`、`modules/`、`technical-decisions/` 中是否还有旧口径
2. 如果转回代码治理：
   - 优先核对 `E` 的通知链与清结算收口是否彻底一致
3. 如果要收尾：
   - 可以基于当前工作区直接提交 `mdl-memory-hub` 的文档同步改动
