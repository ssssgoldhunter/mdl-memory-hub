# 2026-03-27 对话日志

> 当前请优先查看 `docs/superpowers/mdl-supply-chain-abcd/` 目录；本文档保留为当天会话追溯记录。

## 主题

`mdl` 供应链 `A/C/B/D/front` 第二轮收口：修正中断续聊带来的错误概念，把“当前实现基线”和“仍待外部接口/测试补齐项”重新落回文档。

## 本轮关键纠偏

### 1. `A` 的最终边界被重新确认

- `A` 不是 `front` 先查本地再拦截
- `A` 的新增规则校验放在提现 `LiteFlow` 中
- 节点位置：
  - 常规 check 之后
  - 正式提现主交易之前
- 节点名称：
  - `withDrawRuleCheck`
- 当前节点内只保留：
  - 调用清结算查询接口的 `TODO`
  - 开关与日志占位
- 原因：
  - 清结算查询 API 目前还没有提供
- `front` 在 `A` 里只承担：
  - 提现最终结果通知
  - 不承担这次提现前规则校验

### 2. `B` 的“入口复用、核心独立”边界被重新确认

- `B` 对外入口可以挂在现有消费 controller / service 上
- 但 `B` 核心交易链不能复用普通消费主链
- 允许复用的仅限：
  - 通用校验
  - 通用上下文
  - 通知基础设施
- `B` 当前已经调整为：
  - 使用原 `TransSlot`
  - 独立 `deduction` 流程组件目录
  - 独立 `B after`
- 已删除错误概念：
  - `DeductionTransSlot`

### 3. `C` 的冻结额度池语义重新核实

- 原冻结 / 解冻继续使用业务主流水
- 解冻只认：
  - `orgTransNo`
- 不再扩展额外“旧冻结流水号”字段
- 当前代码口径下：
  - 支持按剩余冻结额度做部分解冻
  - 支持按剩余冻结额度做部分冻结扣款
  - 原冻结全部释放后，原记录 `allowFlag` 关闭
- 原冻结额度全部被使用后，原记录 `allowFlag` 关闭
- 因此旧文档里“部分能力未定”或“必须整笔解冻/整笔扣减”的说法，不再作为当前实现基线

### 4. `D` 的 `02` 模式边界重新确认

- `02` 不是复用 `01` 全链路
- `02` 只复用 `01` 中间那段：
  - `batchAddTransferTiData`
- `02` 接口阶段只做：
  - `transTransferTiPre`
  - 批量上传明细
- 真正执行仍由 task 完成
- 不再保留对外的 `registerMode02Detail / batchRegisterMode02Detail`
- `TRANSFER_MODE_SWITCH` 作为参数表开关，优先于请求里的 `transferMode`

### 5. `front` 的当前职责重新确认

- `A`
  - 当前不承担提现前查询校验
  - 只保留提现结果通知职责
- `B`
  - 扣款成功后的通知已走独立 topic
- `D`
  - 划付成功后的通知已走独立 topic
- `front` 的 `B/D` 通知消费端继续复用既有 HTTP consumer 框架
- `24` 小时窗口内持续重发已作为当前实现基线保留

## 本轮实际收口

### 1. 删除已确认废弃的 `D02` 注册接口概念

- 删除：
  - `TransferMode02TaskRegisterReq`
  - `registerMode02Detail(...)`
  - `batchRegisterMode02Detail(...)`
- 原因：
  - 这套设计已经偏离用户确认的 `02` 边界

### 2. 删除已确认废弃的 `DeductionTransSlot`

- `B` 不再单独分叉 slot
- 统一回到原 `TransSlot`

### 3. 编译验证已恢复

- 已执行：
  - `mvn -Dmaven.repo.local=/tmp/codex-m2 -pl fund-catering-consume/fund-catering-consume-service,fund-catering-web -am -DskipTests compile`
- 结果：
  - `BUILD SUCCESS`

## 当前真正未完成项

### 1. `A` 清结算真实查询仍未接入

- 当前仅有 `withDrawRuleCheck` 占位节点
- 等清结算提供查询 API 后，再补真实查询与拦截

### 2. 自动化测试仍未补

- 当前只做了聚焦编译验证
- 单测 / 集成测试仍待补齐

### 3. 联调与数据级验证仍待执行

- 包括：
  - `B useFrozen=true`
  - `D 02 + useFrozen=true`
  - `D 02 + isFrozen=true`
  - `front` 通知失败重发

## 下次续接建议

1. 以 `requirements/2026-03-27-abcd-front-implementation-baseline.md` 作为新的实现基线
2. 联调时若发现行为偏差，按 `receiving-code-review` 流程逐项修
3. 清结算查询 API 一旦提供，先补 `A` 的 `withDrawRuleCheck`
